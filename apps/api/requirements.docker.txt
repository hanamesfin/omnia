# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Settings & validation
pydantic==2.7.1
pydantic-settings==2.3.1

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
pgvector==0.3.1

# Redis + job queue
redis==5.0.4
celery==5.4.0
kombu==5.3.7

# Vector store client
qdrant-client==1.9.1

# LLM
openai==1.30.1
tiktoken==0.7.0

# Embeddings (local, offline-capable)
sentence-transformers==3.0.0
torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP
httpx==0.27.0

# File parsing (agent file_parse tool)
pypdf==4.2.0
python-docx==1.1.2
openpyxl==3.1.2

# Model Context Protocol (MCP servers + agent runner as MCP client)
mcp[cli]>=1.27,<2

# Secure code sandbox (Firecracker micro-VMs)
e2b-code-interpreter>=1.0,<3

# Cursor AI SDK (optional — coding agents call Cursor local/cloud)
cursor-sdk>=0.1.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0

# Utils
python-dotenv==1.0.1
structlog==24.2.0
